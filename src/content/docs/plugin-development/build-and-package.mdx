---
title: Build and Package
description: Turn plugin source code into an uploadable ZIP artifact.
---

This page should cover the last mile before runtime upload.

The story should be: validate imports, expose the plugin class, run the build command, inspect the generated ZIP, upload it to Ocelescope, and diagnose packaging errors.

Mention any checks performed by the build tool, especially restrictions that prevent runtime import failures.
