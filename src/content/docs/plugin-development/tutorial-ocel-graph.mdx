---
title: "Tutorial: OCEL Graph"
description: Build a complete plugin that visualizes an OCEL neighborhood graph.
---

This page should be the hands-on tutorial.

The story should walk through a complete plugin: plan inputs, accept an OCEL, define configuration fields for root and depth, create a custom graph resource, add a visualization, implement the method, build the ZIP, upload it, and run it.

This should become the canonical end-to-end example for new plugin authors.
