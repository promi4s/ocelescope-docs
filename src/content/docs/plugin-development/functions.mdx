---
title: Functions
description: Expose plugin methods and generated input forms.
---

This page should document plugin methods.

The story should be: decorate Python methods, use type hints for OCELs and resources, use one structured `PluginInput`, define Pydantic fields, add OCEL-dependent fields, use computed selections, and return typed OCELs or resources.

This page is central because it explains how Python declarations become frontend forms.
