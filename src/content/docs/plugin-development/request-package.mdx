---
title: Request Additional Package
description: Ask for packages to be added to the plugin runtime environment.
---

This page should explain how plugin authors request runtime packages.

The story should ask for the package name, version constraints, why the dependency is needed, plugin or method context, license considerations, and whether a lighter alternative exists.

The page should clarify that dependency requests affect the shared plugin environment and therefore need review.
