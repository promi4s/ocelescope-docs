---
title: Resources
description: Define serializable artifacts that plugin methods can produce and consume.
---

This page should teach plugin authors how to model outputs.

The story should cover custom `Resource` classes, JSON-serializable fields, nested Pydantic models, annotations, resource reuse across plugins, upload/download behavior, and optional `visualize()` methods.

Keep examples small and focused on patterns authors will reuse.
