---
title: Installation
description: Run Ocelescope locally with Docker and Docker Compose.
---

This page should explain the recommended runtime setup for normal users.

The story should be: Ocelescope runs as a backend service plus a frontend service, Docker Compose wires them together, and a persistent plugin volume keeps uploaded plugins available across restarts.

Include the minimal Compose file, startup command, expected local URL, and how to stop or update the containers.
