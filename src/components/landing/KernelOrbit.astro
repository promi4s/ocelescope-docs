<div class="kernel-orbit not-content">
	<div class="kernel">
		<strong>Ocelescope kernel</strong>
		<span>logs, sessions, resources, execution</span>
	</div>
	<ul>
		<li>OCEL import</li>
		<li>ZIP plugins</li>
		<li>Generated forms</li>
		<li>Visualizations</li>
		<li>Exports</li>
	</ul>
</div>

<style>
	.kernel-orbit {
		display: grid;
		gap: 1rem;
		border: 1px solid var(--sl-color-gray-5);
		border-radius: 8px;
		padding: 1rem;
		background: var(--sl-color-black);
	}

	.kernel {
		display: grid;
		place-items: center;
		min-height: 9rem;
		border: 1px solid var(--sl-color-text-accent);
		border-radius: 8px;
		text-align: center;
	}

	.kernel strong {
		color: var(--sl-color-white);
		font-size: var(--sl-text-h4);
	}

	.kernel span {
		color: var(--sl-color-gray-3);
	}

	ul {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.6rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	li {
		border: 1px solid var(--sl-color-gray-5);
		border-radius: 6px;
		padding: 0.65rem;
		background: var(--sl-color-gray-6);
		color: var(--sl-color-gray-2);
		font-weight: 700;
	}

	@media (max-width: 36rem) {
		ul {
			grid-template-columns: 1fr;
		}
	}
</style>
